# Improvement Plans

Generated by the `improve` skill. Plans are self-contained implementation specs meant for different models/agents to execute.

**Base commit:** `15bda47`

## Execution Order

| Order | Plan | Status | Dependencies |
|-------|------|--------|-------------|
| 1 | **001-split-large-test-files** — Split `server/hermes/runtime.test.ts` (768 lines) and `server/settings/agent-skills/deploy.test.ts` (715 lines) into smaller files by logical concern | Done | None |
| 2 | **002-ci-coverage-threshold** — Add Vitest coverage config with thresholds and CI step to prevent coverage regressions | Done | 001 (recommended to reduce noise) |
| 3 | **003-remaining-route-tests** — Add tests for 5 uncovered routes: `__root`, `index`, `login`, `servers.$id.install`, `servers.new` | Done | None |

## Dependency Graph

```text
001 ──► 002 (recommended, not required)
003 (independent)
```

Plans 001 and 003 can be executed in any order or in parallel. Plan 002 tolerates the large test file noise from 001, but the coverage baseline is cleaner after the split.

## Status Legend

| Status | Meaning |
|--------|---------|
| Proposed | Written but not yet accepted or executed |
| In Progress | Being worked on by an executor |
| Done | Executed, verified, and merged |
| Blocked | Can't proceed — details in plan |
| Stale | Superseded by later changes |
| Rejected | Decided not to do — rationale in plan |
