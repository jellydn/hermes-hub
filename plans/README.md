# Implementation Plans

Generated by the `improve` skill on 2026-06-14, against commit `29c3b46`.
Standard-effort audit. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                              | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------ | -------- | ------ | ---------- | ------ |
| 001  | Test AES-256-GCM crypto helpers (`server/crypto.ts`)               | P1       | S      | —          | DONE   |
| 002  | Fix swallowed download failures in scanner-bypass skill install    | P1       | S      | —          | DONE   |
| 003  | Enforce host-key pinning fail-closed on credential-bearing SSH ops | P1       | S      | —          | DONE   |
| 004  | Test the auth/ownership guard layer (`server/request-guards.ts`)   | P2       | S      | —          | DONE   |
| 005  | Test credential resolution (`server/server-records.ts`)            | P2       | S      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- All five plans are independent and can run in any order / in parallel.
- 004 and 005 are pure additive test coverage; do them first if you want a
  safety net before the larger `DEBT-01` guard-consolidation refactor (not yet
  planned — see below).

## Findings considered but NOT planned (this run)

These are real and recorded so they aren't re-audited, but were left unplanned
as lower leverage or larger/riskier than the top 5:

- **TEST-02 — `server/telegram/model-access.ts` untested (M):** 426 lines of
  cross-tenant access-control + key-decryption logic, fully mocked in
  `telegram.test.ts`. High value but M-effort (needs DB/`getDb` harness). Strong
  candidate for the next batch.
- **TEST-04 — `server/settings/agent-skills/remote.ts` command builders untested (S):**
  `readRemoteManifest` name-validation + `buildDirectSkillInstallCommand` /
  `buildCustomSkillFileWrite` quoting. Pairs naturally with plan 002.
- **DEBT-01 — Two auth-guard styles (M):** ~8 handlers still hand-roll
  `getAuthSession` + manual ownership checks instead of `request-guards.ts`
  helpers. Land plan 004 first as the safety net, then migrate.
- **DEBT-02 — Oversized modules (L):** `server/telegram.ts` (639), `server/servers.ts`
  (564), `src/features/dashboard/status-overview.tsx` (651). Multi-day extraction.
- **DEBT-03 — Duplicate `keepLatestPerGroup` / `keepLatestPerSubscriptionGroup`
  in `model-access.ts` (S):** byte-identical except the key field; fold into one
  generic. Best landed alongside TEST-02.
- **PERF-01 — No SSH connection reuse (M):** connection-per-operation to each VPS.
  Likely an accepted single-instance MVP tradeoff (ADR 0009). Verify fleet size
  before investing.
- **PERF-02 — `installs` missing composite `(serverId, createdAt desc)` index (S):**
  negligible at current row counts; pre-emptive only.
- **DEBT-04 — Install progress uses SSE + 3s polling in parallel (M, LOW conf):**
  probably intentional (events vs. log tail); investigate before changing.
- **DEP — esbuild dev-server advisories (transitive via drizzle-kit/vite/tsx):**
  `bun audit` reports 1 high / 1 moderate / 1 low, all scoped to the esbuild dev
  server, not the production runtime. Resolve via routine dependency bumps; not a
  production exposure.

## Considered and rejected (not findings)

- MCP `command`/`args` executed by remote Hermes runtime — by design; the user
  configures execution on infrastructure they own (YAML-serialized, not shell-injected).
- web-ui reverse proxy same-origin + `set-cookie` passthrough — settled by ADR 0011.
- Magic-link URL `console.log` in `server/lib/send-magic-link-email.ts` — gated;
  production throws instead of logging the token.
- `src/routes/servers.$id.tsx` `useMountEffect` client fetch — documented
  intentional exception in `AGENTS.md` / `CONCERNS.md`.
- `server/crypto.ts` legacy plaintext fallback, `auth.ts` dev-secret fallback,
  HTTPS guard trusting `x-forwarded-proto`, in-memory rate limiter / single-instance
  state — all documented in `.planning/codebase/CONCERNS.md` as accepted constraints.
