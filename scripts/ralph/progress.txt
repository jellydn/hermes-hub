## Codebase Patterns
- TanStack Start route types come from the generated `src/routeTree.gen.ts`; run a build or dev server after adding routes so typecheck can see the generated tree.
- This repo uses Bun as the package manager; keep `bun.lock` and do not add `package-lock.json`.
- Reusable UI primitives follow the shadcn layout: place components in `src/components/ui/` and compose class names with the shared `cn()` helper from `src/lib/utils.ts`.
- Server-side request branching lives in `src/server.ts`; use it to intercept `/api/*` requests and pass all page requests through the default TanStack Start handler.
- `drizzle.config.ts` throws if `DATABASE_URL` is missing, so export that env var before running `drizzle-kit generate` or migration commands.
- Better Auth mounts directly on the Hono app in `server/app.ts`; if the product wants friendlier `/api/auth/*` aliases, implement them as thin request rewrites to Better Auth's built-in routes instead of duplicating auth logic.
- Better Auth setup must stay lazy in this app: construct it through a getter, not at module load, so SSR routes can still render without `DATABASE_URL`, and give the client an absolute auth base URL because Better Auth rejects relative `baseURL` values during SSR.

## 2026-05-26 09:41:15 +08 - US-001
[Session: https://opncd.ai/s/]
- Scaffolded the HermesHub app with TanStack Start, TailwindCSS v4, and the default starter routes/components.
- Added a `typecheck` script, kept the repo on Bun with `bun.lock`, and updated root docs/agent notes to match the generated `src/` layout.
- Verified `npm run dev`, `npm run build`, `npm run typecheck`, and `npm test` all pass for the scaffold.
- Files changed: `AGENTS.md`, `README.md`, `package.json`, `bun.lock`, `.gitignore`, `.cta.json`, `.pre-commit-config.yaml`, `.vscode/settings.json`, `vite.config.ts`, `tsconfig.json`, `src/**`, `public/**`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - TanStack Start route typing depends on the generated `src/routeTree.gen.ts`; if route imports look broken, regenerate by running build or dev.
  - The project standard is Bun even if an upstream scaffold uses npm defaults, so normalize lockfiles immediately.
  - The scaffold already added `opencode.json` with Chrome DevTools MCP config, so future frontend stories can use browser verification.
---

## 2026-05-26 09:50:06 +0800 - US-002
[Session: https://opncd.ai/s/]
- Added shadcn-style project configuration (`components.json`), supporting dependencies, a shared `cn()` utility, and a reusable `Button` component under `src/components/ui/`.
- Updated the home page CTA to render the new `Button` component variants so Tailwind utilities and the shadcn primitive are visible in the default route.
- Verified `bun run typecheck`, `bun run build`, and `bun run test` pass, then confirmed the button renders in the browser at `http://localhost:3000/`.
- Files changed: `AGENTS.md`, `package.json`, `bun.lock`, `components.json`, `postcss.config.mjs`, `src/lib/utils.ts`, `src/components/ui/button.tsx`, `src/routes/index.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - The `shadcn init` CLI can still prompt interactively in this TanStack Start setup, so keeping the generated config files checked in avoids repeated manual setup.
  - Keep reusable UI primitives in `src/components/ui/` and point feature pages at those components instead of duplicating Tailwind class strings.
  - Browser verification can reuse the already running app on port `3000`; the scaffold's `opencode.json` Chrome MCP setup works as expected.
---

## 2026-05-26 09:55:00 +0800 - US-003
[Session: https://opncd.ai/s/]
- Added Drizzle ORM and PostgreSQL wiring with `drizzle.config.ts`, a minimal `server/db/` module, and an initial generated SQL migration under `drizzle/`.
- Added a custom `src/server.ts` entry that routes `/api/*` requests into a Hono app while preserving the default TanStack Start handler for everything else.
- Implemented `GET /api/health` JSON status reporting plus a Vitest coverage check for the Hono app, and verified `bun run typecheck`, `bun run test`, `bun run build`, `bunx @biomejs/biome check AGENTS.md README.md package.json drizzle.config.ts server src scripts/ralph`, and `DATABASE_URL=postgres://postgres:postgres@localhost:5432/hermeshub bunx drizzle-kit generate`.
- Files changed: `AGENTS.md`, `package.json`, `bun.lock`, `drizzle.config.ts`, `drizzle/**`, `server/**`, `src/server.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - TanStack Start custom server logic should stay in `src/server.ts`; branch API paths there instead of trying to mix API routing into file-based page routes.
- The published `@hono/tanstack-start` package was not available from npm during setup, so plain `hono` plus the TanStack Start server entry is the working integration path here.
- `GET /api/health` can report database degradation in the JSON body without failing the route itself, which keeps a stable monitoring endpoint while DB setup is still in progress.
---

## 2026-05-26 10:02:28 +0800 - US-004
[Session: https://opncd.ai/s/]
- Added Better Auth server configuration with the Drizzle adapter and magic-link plugin, using console logging as the MVP email transport.
- Mounted Better Auth on the Hono API app and added `/api/auth/send-magic-link` and `/api/auth/verify-magic-link` aliases that rewrite into Better Auth's built-in magic-link endpoints.
- Extended the Drizzle schema with Better Auth tables, generated the auth migration under `drizzle/`, and added tests that verify the auth aliases route through the Better Auth handler.
- Files changed: `AGENTS.md`, `package.json`, `bun.lock`, `server/auth.ts`, `server/app.ts`, `server/app.test.ts`, `server/db/schema.ts`, `drizzle/0002_perfect_caretaker.sql`, `drizzle/meta/_journal.json`, `drizzle/meta/0002_snapshot.json`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Better Auth's current Hono integration is just `auth.handler()` on a Hono route; the older `@better-auth/hono-adapter` package name in the PRD is no longer published.
  - Route order matters in Hono: register specific auth aliases before the catch-all `/auth/*` handler or the aliases will never run.
  - Let `drizzle-kit generate` author the migration snapshot/journal files after schema changes instead of hand-editing Drizzle metadata.
---

## 2026-05-26 10:12:10 +0800 - US-005
[Session: https://opncd.ai/s/]
- Added passwordless auth UI and route protection with new `/login` and `/dashboard` routes, redirecting unauthenticated dashboard visits back to login with a redirect target.
- Added a shared Better Auth client/session bridge, wired the header to show login/logout state and the authenticated email, and added `/api/auth/callback` as a friendly verify alias.
- Made auth initialization lazy so SSR routes still render without `DATABASE_URL`; in that state the login form now fails gracefully instead of crashing the page, and browser verification confirmed `/login`, `/dashboard` redirect behavior, and the callback alias response.
- Files changed: `AGENTS.md`, `server/auth.ts`, `server/app.ts`, `server/app.test.ts`, `src/components/Header.tsx`, `src/lib/auth-client.ts`, `src/lib/session.ts`, `src/routes/index.tsx`, `src/routes/login.tsx`, `src/routes/dashboard.tsx`, `src/routeTree.gen.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/us-005-login.png`
- **Learnings for future iterations:**
  - Better Auth's React client needs an absolute `baseURL` during SSR; a relative `/api/auth` base breaks route rendering in TanStack Start.
  - Route guards can reuse a small `createServerFn` wrapper around `auth.api.getSession`, which keeps redirects server-backed instead of duplicating cookie parsing in routes.
  - Local browser verification can still cover login UI and protected-route redirects without a database, but end-to-end magic-link success needs `DATABASE_URL` configured.
---
